[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = [
  'rm -rf node_modules package-lock.json',
  'npm install --legacy-peer-deps',
  'npm install --force rollup'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview -- --host 0.0.0.0 --port 8080'