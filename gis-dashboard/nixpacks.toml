[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = [
  'rm -rf node_modules',
  'npm install --legacy-peer-deps'
]

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run preview'